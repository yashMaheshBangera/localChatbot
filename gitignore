# Data directories -- large, regeneratable from build_dataset.py /
# parse_and_chunk.py / embed_chunks.py, shouldn't live in version control
data/raw/
data/processed/
data/embedded/

# Qdrant loading state -- regeneratable (safe to delete: re-running
# load_qdrant.py just re-upserts everything via idempotent UUIDv5 point IDs)
embedding/.loaded_files.json

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Environment / secrets
.env

# OS / editor cruft
.DS_Store
.vscode/
.idea/
